name: esp_monitor
description: Pure-Dart serial monitor model for ESP-IDF devices — line splitting, ANSI colours, log line parsing and filtering.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
