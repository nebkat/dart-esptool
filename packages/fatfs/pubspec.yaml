name: fatfs
description: Pure-Dart fatfs filesystem image reader (and image builder) for ESP-IDF partitions.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  collection: ^1.18.0

dev_dependencies:
  archive: ^4.0.0
  lints: ^6.0.0
  test: ^1.24.0
